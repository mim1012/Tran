# 공통 UX 원칙 상세 명세

> **적용 범위**: 모든 화면에 일관되게 적용
> **핵심 패턴**: 거래처 탭 → 2컬럼 리스트 → 상세화면 3분할

---

## 1. 거래처 기반 탭 시스템

### 1.1 전체 화면 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│  [발주관리]  [판매관리]  [재고관리]  [정산관리]  ...                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  [+ 거래처 선택]  [A병원 ✕]  [B의원 ✕]  [C병원 ✕]                   │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│                      (선택된 탭의 콘텐츠 영역)                       │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.2 거래처 선택 탭 동작

| 동작 | 설명 |
|------|------|
| **[+ 거래처 선택]** 클릭 | 새 탭 생성 → 거래처 리스트 표시 |
| **거래처 클릭** | 해당 거래처 상세화면으로 전환 |
| **탭 ✕ 클릭** | 해당 탭 닫기 |
| **탭 전환** | 각 거래처별 독립 상태 유지 |

### 1.3 거래처 선택 탭 (2컬럼 리스트)

```
┌─────────────────────────────────────────────────────────────────────┐
│  [+ 거래처 선택]  [A병원 ✕]                                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ 🔍 [업체명 검색...]                      [유형 ▼] [정렬 ▼]   │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────┬─────────────────────────┐             │
│  │                         │                         │             │
│  │  🏥 A병원               │  🏥 B의원               │             │
│  │  서울시 강남구          │  서울시 서초구          │             │
│  │  최근거래: 2025-01-22   │  최근거래: 2025-01-20   │             │
│  │                         │                         │             │
│  ├─────────────────────────┼─────────────────────────┤             │
│  │                         │                         │             │
│  │  🏢 C도매               │  🏢 D본사               │             │
│  │  경기도 성남시          │  부산시 해운대구        │             │
│  │  최근거래: 2025-01-18   │  최근거래: 2025-01-15   │             │
│  │                         │                         │             │
│  ├─────────────────────────┼─────────────────────────┤             │
│  │                         │                         │             │
│  │  🏥 E병원               │  🏥 F의원               │             │
│  │  대전시 유성구          │  광주시 북구            │             │
│  │  최근거래: 2025-01-10   │  최근거래: 2025-01-05   │             │
│  │                         │                         │             │
│  └─────────────────────────┴─────────────────────────┘             │
│                                                                     │
│  ◀  1  2  3  ...  10  ▶                          총 156개 거래처   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.4 거래처 카드 컴포넌트

```csharp
public class CompanyCardViewModel
{
    public int Id { get; set; }
    public string Name { get; set; }
    public string TypeIcon { get; set; }        // 🏥 병원, 🏢 도매
    public string Address { get; set; }
    public DateTime? LastTransactionDate { get; set; }
    public int ActiveOrderCount { get; set; }   // 진행 중인 발주
    public decimal? OutstandingAmount { get; set; } // 미수금
}
```

```xml
<!-- CompanyCard.xaml -->
<Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8"
        Padding="16" Margin="8" Cursor="Hand"
        Background="{Binding IsSelected, Converter={StaticResource BoolToBgConverter}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="{Binding TypeIcon}" FontSize="20"/>
            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Margin="8,0"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding Address}"
                   Foreground="Gray" FontSize="12" Margin="0,4"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="최근거래: " Foreground="Gray" FontSize="11"/>
            <TextBlock Text="{Binding LastTransactionDate, StringFormat=yyyy-MM-dd}"
                       FontSize="11"/>
        </StackPanel>
    </Grid>
</Border>
```

---

## 2. 거래처 상세화면 3분할 레이아웃

### 2.1 상세화면 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│  [+ 거래처 선택]  [A병원 ✕]                                          │
├─────────────────────────────────────────────────────────────────────┤
│  A병원 - 발주관리                                    [새 발주 작성]  │
├───────────────────────────────┬─────────────────────────────────────┤
│                               │                                     │
│  📦 품목 리스트               │  ⭐ 최근거래 품목                    │
│  ─────────────────────────    │  ─────────────────────────          │
│                               │                                     │
│  [품목 검색...]               │  자주 시키는 품목 (최근 30일)       │
│                               │                                     │
│  ☐ 테이프 10EA    ₩3,500     │  ☑ 테이프 10EA   수량: [__100__]    │
│  ☐ 거즈 1BOX      ₩12,000    │  ☑ 거즈 1BOX     수량: [___50__]    │
│  ☐ 주사기 100EA   ₩8,000     │  ☐ 주사기 100EA  수량: [______]     │
│  ☐ 소독약 500ml   ₩5,000     │                                     │
│  ☐ 붕대 10M       ₩2,000     │  ──────────────────────────────     │
│  ...                          │  합계: ₩950,000                     │
│                               │  [임시저장]  [발주서 보내기]        │
│                               │                                     │
├───────────────────────────────┴─────────────────────────────────────┤
│                                                                     │
│  📋 최근 거래 내역                                                   │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ [등록]  [최근 작업 (임시저장)]  [최근 성사 내역]             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  (선택된 탭의 내용)                                                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 3분할 영역 설명

| 영역 | 위치 | 설명 |
|------|------|------|
| **품목 리스트** | 좌상단 | 전체 품목 목록, 검색/필터 |
| **최근거래 품목** | 우상단 | 자주 거래하는 품목, 수량 입력, 발주서 작성 |
| **최근 거래 내역** | 하단 전체 | 3개 탭 (등록, 최근작업, 성사내역) |

### 2.3 하단 3개 탭 상세

#### 2.3.1 [등록] 탭 - 새 문서 작성

```
┌─────────────────────────────────────────────────────────────────────┐
│  [등록]  [최근 작업 (임시저장)]  [최근 성사 내역]                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  새 발주서 작성                                      [템플릿 불러오기] │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ 발주일: [2025-01-22  📅]    희망납품일: [2025-01-25  📅]     │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ No │ 품목명       │ 규격   │ 단가      │ 수량  │ 금액        │ │
│  ├────┼──────────────┼────────┼───────────┼───────┼─────────────┤ │
│  │ 1  │ 테이프 10EA  │ 의료용 │ ₩3,500   │ 100   │ ₩350,000   │ │
│  │ 2  │ 거즈 1BOX    │ 멸균   │ ₩12,000  │ 50    │ ₩600,000   │ │
│  │ 3  │ [+ 품목 추가]│        │           │       │             │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  비고: [                                                         ] │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│  합계: ₩950,000                      [임시저장]  [발주서 보내기]    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

#### 2.3.2 [최근 작업 (임시저장)] 탭

```
┌─────────────────────────────────────────────────────────────────────┐
│  [등록]  [최근 작업 (임시저장)]  [최근 성사 내역]                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  임시저장된 작업                          [정렬: 최신순 ▼] [전체삭제] │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ 📝 발주서 임시저장 #1                         2025-01-22 14:30 │ │
│  │    테이프 10EA × 100, 거즈 1BOX × 50                          │ │
│  │    합계: ₩950,000                                             │ │
│  │    [불러오기]  [삭제]                                          │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │ 📝 발주서 임시저장 #2                         2025-01-21 16:20 │ │
│  │    주사기 100EA × 200                                         │ │
│  │    합계: ₩1,600,000                                           │ │
│  │    [불러오기]  [삭제]                                          │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │ 📝 발주서 임시저장 #3                         2025-01-20 10:15 │ │
│  │    소독약 500ml × 30, 붕대 10M × 100                          │ │
│  │    합계: ₩350,000                                             │ │
│  │    [불러오기]  [삭제]                                          │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  총 3건의 임시저장 항목                                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

#### 2.3.3 [최근 성사 내역] 탭

```
┌─────────────────────────────────────────────────────────────────────┐
│  [등록]  [최근 작업 (임시저장)]  [최근 성사 내역]                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  완료된 거래 내역                    [기간: 최근 30일 ▼] [Excel 내보내기] │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ 날짜       │ 문서번호        │ 품목요약          │ 금액      │ 상태 │ │
│  ├────────────┼─────────────────┼───────────────────┼───────────┼──────┤ │
│  │ 2025-01-20 │ OR-0120-0001   │ 테이프 외 2건     │ ₩850,000 │ ✅완료│ │
│  │ 2025-01-15 │ OR-0115-0002   │ 거즈 외 1건       │ ₩600,000 │ ✅완료│ │
│  │ 2025-01-10 │ OR-0110-0003   │ 주사기 외 3건     │ ₩1.2M    │ ✅완료│ │
│  │ 2025-01-05 │ OR-0105-0001   │ 소독약            │ ₩150,000 │ ✅완료│ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ◀ 1 2 3 ▶                               이 거래처 총 거래: 45건   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. 작업 흐름 (Workflow)

### 3.1 발주 작성 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│   ① 거래처 선택    →    ② 품목/수량 입력    →    ③ 임시저장        │
│                                                                     │
│        ↓                      ↓                      ↓              │
│                                                                     │
│   [거래처 탭 생성]     [최근거래 품목에서      [최근 작업 탭에       │
│   [상세화면 진입]       수량만 입력]            자동 반영]           │
│                                                                     │
│                              ↓                                      │
│                                                                     │
│              ④ 불러오기    →    ⑤ 발주서 보내기                     │
│                                                                     │
│                   ↓                      ↓                          │
│                                                                     │
│           [최근 작업에서         [발주 확정]                        │
│            임시저장 선택]        [최근 성사 내역에 반영]             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 임시저장 데이터 모델

```csharp
public class DraftDocument
{
    public int Id { get; set; }
    public string DraftNumber { get; set; }     // DRAFT-YYYYMMDD-XXXX

    public DocumentType Type { get; set; }      // Order, Sale, Quotation
    public int CompanyId { get; set; }          // 거래처

    public string ContentJson { get; set; }     // 품목, 수량, 단가 등 JSON
    public string PreviewSummary { get; set; }  // "테이프 10EA × 100, 거즈 1BOX × 50"
    public decimal TotalAmount { get; set; }

    public int UserId { get; set; }             // 작성자
    public DateTime CreatedAt { get; set; }
    public DateTime UpdatedAt { get; set; }

    public bool IsDeleted { get; set; }
}

public enum DocumentType
{
    Order = 1,       // 발주
    Sale = 2,        // 판매
    Quotation = 3,   // 견적
    Purchase = 4     // 구매
}
```

### 3.3 임시저장 서비스

```csharp
public class DraftService
{
    /// <summary>
    /// 임시저장
    /// </summary>
    public async Task<DraftDocument> SaveDraftAsync(DraftSaveDto dto)
    {
        var existing = await _context.DraftDocuments
            .FirstOrDefaultAsync(d => d.Id == dto.DraftId && !d.IsDeleted);

        if (existing != null)
        {
            // 기존 임시저장 업데이트
            existing.ContentJson = JsonSerializer.Serialize(dto.Content);
            existing.PreviewSummary = GeneratePreviewSummary(dto.Content);
            existing.TotalAmount = dto.Content.TotalAmount;
            existing.UpdatedAt = DateTime.UtcNow;
        }
        else
        {
            // 새 임시저장
            existing = new DraftDocument
            {
                DraftNumber = await GenerateDraftNumberAsync(),
                Type = dto.Type,
                CompanyId = dto.CompanyId,
                ContentJson = JsonSerializer.Serialize(dto.Content),
                PreviewSummary = GeneratePreviewSummary(dto.Content),
                TotalAmount = dto.Content.TotalAmount,
                UserId = _currentUser.Id,
                CreatedAt = DateTime.UtcNow,
                UpdatedAt = DateTime.UtcNow
            };
            _context.DraftDocuments.Add(existing);
        }

        await _context.SaveChangesAsync();
        return existing;
    }

    /// <summary>
    /// 임시저장 불러오기
    /// </summary>
    public async Task<DraftContentDto> LoadDraftAsync(int draftId)
    {
        var draft = await _context.DraftDocuments
            .FirstOrDefaultAsync(d => d.Id == draftId && !d.IsDeleted);

        if (draft == null)
            throw new DraftNotFoundException(draftId);

        return JsonSerializer.Deserialize<DraftContentDto>(draft.ContentJson);
    }

    /// <summary>
    /// 거래처별 임시저장 목록
    /// </summary>
    public async Task<List<DraftSummaryDto>> GetDraftsByCompanyAsync(
        int companyId,
        DocumentType? type = null)
    {
        var query = _context.DraftDocuments
            .Where(d => d.CompanyId == companyId && !d.IsDeleted);

        if (type.HasValue)
            query = query.Where(d => d.Type == type);

        return await query
            .OrderByDescending(d => d.UpdatedAt)
            .Select(d => new DraftSummaryDto
            {
                Id = d.Id,
                DraftNumber = d.DraftNumber,
                Type = d.Type,
                PreviewSummary = d.PreviewSummary,
                TotalAmount = d.TotalAmount,
                UpdatedAt = d.UpdatedAt
            })
            .ToListAsync();
    }

    /// <summary>
    /// 임시저장 → 정식 문서 전환
    /// </summary>
    public async Task<int> PromoteDraftAsync(int draftId)
    {
        var draft = await _context.DraftDocuments
            .FirstOrDefaultAsync(d => d.Id == draftId && !d.IsDeleted);

        // 임시저장 삭제 (soft delete)
        draft.IsDeleted = true;
        await _context.SaveChangesAsync();

        return draft.Id;
    }

    private string GeneratePreviewSummary(DraftContentDto content)
    {
        var items = content.Items.Take(3)
            .Select(i => $"{i.ProductName} × {i.Quantity}");

        var summary = string.Join(", ", items);

        if (content.Items.Count > 3)
            summary += $" 외 {content.Items.Count - 3}건";

        return summary;
    }
}
```

### 3.4 발주 완료 흐름

```csharp
public class OrderSubmissionService
{
    /// <summary>
    /// 발주서 보내기 (임시저장 → 정식 발주)
    /// </summary>
    public async Task<Order> SubmitOrderAsync(OrderSubmitDto dto)
    {
        Order order;

        // 임시저장에서 불러온 경우
        if (dto.DraftId.HasValue)
        {
            var draftContent = await _draftService.LoadDraftAsync(dto.DraftId.Value);
            order = await CreateOrderFromDraftAsync(draftContent, dto);

            // 임시저장 삭제
            await _draftService.PromoteDraftAsync(dto.DraftId.Value);
        }
        else
        {
            // 직접 작성한 경우
            order = await CreateOrderAsync(dto);
        }

        // 상태 전이: 요청
        order.State = OrderState.Requested;
        await _context.SaveChangesAsync();

        // 품목 통계 업데이트 (최근거래 품목 반영)
        foreach (var item in order.Items)
        {
            await _frequentProductService.UpdateOrderStatisticsAsync(
                order.ToCompanyId,
                item.ProductId);
        }

        // 알림 발송
        await _notificationService.NotifyNewOrderAsync(order);

        return order;
    }
}
```

---

## 4. 최근거래 품목 시스템

### 4.1 자주 거래하는 품목 조회

```csharp
public class FrequentProductService
{
    /// <summary>
    /// 거래처별 최근 거래 품목 (최근 30일 기준)
    /// </summary>
    public async Task<List<FrequentProductDto>> GetFrequentProductsAsync(
        int companyId,
        int limit = 10)
    {
        var since = DateTime.Today.AddDays(-30);

        // 최근 30일 거래에서 품목별 빈도 계산
        var productStats = await _context.OrderItems
            .Include(oi => oi.Order)
            .Where(oi => oi.Order.ToCompanyId == companyId)
            .Where(oi => oi.Order.CreatedAt >= since)
            .GroupBy(oi => oi.ProductId)
            .Select(g => new
            {
                ProductId = g.Key,
                OrderCount = g.Count(),
                TotalQuantity = g.Sum(x => x.Quantity),
                LastOrderDate = g.Max(x => x.Order.CreatedAt),
                AvgQuantity = (int)g.Average(x => x.Quantity)
            })
            .OrderByDescending(x => x.OrderCount)
            .Take(limit)
            .ToListAsync();

        var productIds = productStats.Select(x => x.ProductId).ToList();
        var products = await _context.Products
            .Where(p => productIds.Contains(p.Id))
            .ToDictionaryAsync(p => p.Id);

        return productStats.Select(stat => new FrequentProductDto
        {
            ProductId = stat.ProductId,
            ProductName = products[stat.ProductId].Name,
            Specification = products[stat.ProductId].Specification,
            Unit = products[stat.ProductId].Unit,
            CurrentPrice = await GetCurrentPriceAsync(companyId, stat.ProductId),
            OrderCount = stat.OrderCount,
            LastOrderDate = stat.LastOrderDate,
            SuggestedQuantity = stat.AvgQuantity  // 평균 수량 제안
        }).ToList();
    }
}
```

### 4.2 최근거래 품목 UI

```xml
<!-- FrequentProductsPanel.xaml -->
<Border BorderBrush="#E0E0E0" BorderThickness="1" Padding="12">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <TextBlock Grid.Row="0" Text="⭐ 최근거래 품목" FontWeight="Bold" FontSize="14"/>
        <TextBlock Text="자주 시키는 품목 (최근 30일)" Foreground="Gray" FontSize="11"/>

        <!-- 품목 리스트 -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding FrequentProducts}" Margin="0,12">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>

                        <StackPanel Grid.Column="1" Margin="8,0">
                            <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding CurrentPrice, StringFormat=₩{0:#,##0}}"
                                       Foreground="Gray" FontSize="11"/>
                        </StackPanel>

                        <TextBox Grid.Column="2"
                                 Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"
                                 IsEnabled="{Binding IsSelected}"
                                 HorizontalContentAlignment="Right"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 합계 -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="0,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="합계: " FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalAmount, StringFormat=₩{0:#,##0}}"
                           FontWeight="Bold" FontSize="16"/>
            </StackPanel>
        </Border>

        <!-- 버튼 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="임시저장" Command="{Binding SaveDraftCommand}"
                    Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
            <Button Content="발주서 보내기" Command="{Binding SubmitOrderCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Border>
```

---

## 5. 데이터 변경 하이라이트 시스템

### 5.1 상태별 색상 정의

```csharp
public static class ChangeHighlightColors
{
    // 신규 데이터
    public static readonly Color NewBackground = Color.FromHex("#E6F4EA");
    public static readonly Color NewText = Color.FromHex("#1E7F34");
    public static readonly Color NewBorder = Color.FromHex("#34A853");

    // 수정된 데이터
    public static readonly Color ModifiedBackground = Color.FromHex("#FFF4E5");
    public static readonly Color ModifiedText = Color.FromHex("#E67700");
    public static readonly Color ModifiedBorder = Color.FromHex("#FBBC04");

    // 취소/무효 데이터
    public static readonly Color CancelledBackground = Color.FromHex("#F0F0F0");
    public static readonly Color CancelledText = Color.FromHex("#555555");
    public static readonly Color CancelledBorder = Color.FromHex("#9E9E9E");
}
```

### 5.2 리스트 행 스타일

```xml
<!-- App.xaml -->
<Style x:Key="NewRowStyle" TargetType="DataGridRow">
    <Setter Property="Background" Value="#E6F4EA"/>
    <Setter Property="Foreground" Value="#1E7F34"/>
</Style>

<Style x:Key="ModifiedRowStyle" TargetType="DataGridRow">
    <Setter Property="Background" Value="#FFF4E5"/>
    <Setter Property="Foreground" Value="#E67700"/>
</Style>

<Style x:Key="CancelledRowStyle" TargetType="DataGridRow">
    <Setter Property="Background" Value="#F0F0F0"/>
    <Setter Property="Foreground" Value="#555555"/>
    <Setter Property="TextBlock.TextDecorations" Value="Strikethrough"/>
    <Setter Property="Opacity" Value="0.7"/>
</Style>
```

---

## 6. 공통 리스트 기능

### 6.1 2컬럼 그리드 레이아웃 (거래처 리스트용)

```xml
<!-- TwoColumnGrid.xaml -->
<ItemsControl ItemsSource="{Binding Items}">
    <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
            <UniformGrid Columns="2"/>
        </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>
</ItemsControl>
```

### 6.2 필터 시스템

```csharp
public class CompanyFilterService
{
    public async Task<List<CompanyCardViewModel>> FilterCompaniesAsync(CompanyFilterDto filter)
    {
        var query = _context.Companies.AsQueryable();

        // 업체명 검색
        if (!string.IsNullOrWhiteSpace(filter.SearchTerm))
        {
            query = query.Where(c => c.Name.Contains(filter.SearchTerm));
        }

        // 유형 필터
        if (filter.Type.HasValue)
        {
            query = query.Where(c => c.Type == filter.Type);
        }

        // 정렬
        query = filter.SortBy switch
        {
            "name" => query.OrderBy(c => c.Name),
            "recent" => query.OrderByDescending(c => c.LastTransactionDate),
            "amount" => query.OrderByDescending(c => c.TotalTransactionAmount),
            _ => query.OrderBy(c => c.Name)
        };

        return await query
            .Select(c => new CompanyCardViewModel
            {
                Id = c.Id,
                Name = c.Name,
                TypeIcon = c.Type == CompanyType.Hospital ? "🏥" : "🏢",
                Address = c.Address,
                LastTransactionDate = c.LastTransactionDate
            })
            .ToListAsync();
    }
}
```

### 6.3 Excel 내보내기

```csharp
public class ExcelExportService
{
    public async Task<byte[]> ExportToExcelAsync<T>(
        IEnumerable<T> data,
        ExportOptions options)
    {
        using var package = new ExcelPackage();
        var worksheet = package.Workbook.Worksheets.Add(options.SheetName);

        var properties = typeof(T).GetProperties()
            .Where(p => options.IncludedColumns.Contains(p.Name));

        // 헤더
        int col = 1;
        foreach (var prop in properties)
        {
            worksheet.Cells[1, col].Value = GetDisplayName(prop);
            worksheet.Cells[1, col].Style.Font.Bold = true;
            col++;
        }

        // 데이터
        int row = 2;
        foreach (var item in data)
        {
            col = 1;
            foreach (var prop in properties)
            {
                worksheet.Cells[row, col].Value = prop.GetValue(item);
                col++;
            }
            row++;
        }

        worksheet.Cells.AutoFitColumns();
        return await package.GetAsByteArrayAsync();
    }
}
```

---

## 7. 품목 선택 모달 (공통 컴포넌트)

> **상세 명세**: `docs/features/05-product-master.md`

### 7.1 품목 선택 모달 개요

품목 선택 모달은 발주, 판매, 견적서 등 모든 화면에서 공통으로 사용되는 핵심 컴포넌트입니다.

| 호출 화면 | 품목 유형 필터 | 표시 정보 |
|-----------|----------------|-----------|
| **발주** | 📥 구매 품목 (Buy + Both) | 구매 단가, 거래처 |
| **판매** | 📤 판매 품목 (Sell + Both) | 판매 단가, 재고 |
| **견적서** | 📤 판매 품목 (Sell + Both) | 판매 단가 |
| **재고** | 📤 판매 품목 (Sell + Both) | 재고 수량 |

### 7.2 모달 UI

```
┌─────────────────────────────────────────────────────────────────────┐
│  📤 판매 품목 선택                                             [✕]  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ 🔍 [품목명, 코드, 바코드 검색...]                [카테고리 ▼] │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │   │ 유형│ 코드  │ 품목명       │ 규격  │ 단위│ 단가   │ 재고 │ │
│  ├───┼────┼───────┼──────────────┼───────┼─────┼────────┼──────┤ │
│  │ ○ │ 📤 │ P-001 │ 테이프 10EA  │ 의료용│ EA  │ ₩3,500│ 500  │ │
│  │ ○ │ 📤 │ P-002 │ 거즈 1BOX    │ 멸균  │ BOX │ ₩12,000│ 200 │ │
│  │ ● │ ↔️ │ P-003 │ 주사기 100EA │ 일반  │ EA  │ ₩8,000│ 150  │ │
│  │ ○ │ 📤 │ P-004 │ 소독약 500ml │ 에탄올│BOTTLE│ ₩5,000│ 80  │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  선택: 주사기 100EA (₩8,000, 재고 150)                              │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 수량: [        10        ]  금액: ₩80,000                   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│                              [취소]  [품목 추가]                    │
│  💡 원하는 품목이 없나요?  [+ 새 품목 등록]                          │
└─────────────────────────────────────────────────────────────────────┘
```

### 7.3 모달 호출 방법

```csharp
// 발주 화면에서 호출 (구매 품목만)
var modal = ProductSelectionModal.ForOrder();
modal.OnProductSelected += (sender, args) => AddProductToOrder(args);
modal.Show();

// 판매 화면에서 호출 (판매 품목만)
var modal = ProductSelectionModal.ForSale();
modal.OnProductSelected += (sender, args) => AddProductToSale(args);
modal.Show();

// 견적서 화면에서 호출 (판매 품목만)
var modal = ProductSelectionModal.ForQuotation();
modal.OnProductSelected += (sender, args) => AddProductToQuotation(args);
modal.Show();
```

### 7.4 품목 유형 아이콘

| 아이콘 | 의미 | 사용처 |
|--------|------|--------|
| 📥 | 구매 품목 (Buy) | 발주에서만 사용 |
| 📤 | 판매 품목 (Sell) | 판매, 견적서, 재고에서 사용 |
| ↔️ | 둘다 (Both) | 모든 화면에서 사용 |

---

## 8. 키보드 단축키

| 단축키 | 동작 | 적용 화면 |
|--------|------|-----------|
| `Ctrl + N` | 새 거래처 탭 열기 | 전체 |
| `Ctrl + S` | 임시저장 | 등록 탭 |
| `Ctrl + Enter` | 발주서 보내기 | 등록 탭 |
| `Ctrl + F` | 검색 포커스 | 리스트 |
| `Ctrl + W` | 현재 탭 닫기 | 전체 |
| `Esc` | 취소/닫기 | 다이얼로그 |
| `F5` | 새로고침 | 전체 |
